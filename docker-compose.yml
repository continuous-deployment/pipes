php:
  image: registry.noprobe.co.uk/nginx-phpfpm
  ports:
    - "9000:80"
  volumes:
    - .:/var/www
    - .docker/nginx/sites-enabled:/etc/nginx/sites-enabled
  links:
    - mysql:mysql

mysql:
  image: dydx/alpine-mariadb
  ports:
    - "3309:3306"
  volumes:
    - .docker/db/data:/var/lib/mysql

runner:
  image: registry.noprobe.co.uk/pipes-runner
  volumes_from:
    - php
  links:
    - mysql:mysql